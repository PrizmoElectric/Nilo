SESSION RESUME — 2026-05-29 (session 9, in progress)
=============================================
Pending items: agent/CHECKLIST.txt
Full architecture: agent/map.txt
Detailed history: agent/CHANGELOG.txt

── OBJECTIVE FOR NEXT SESSION ────────────────────────────────────────────────

  NILO V1 — Node.js bot, no learning-mode constraints. Just implement.
  (Nilo v2 Python rewrite is a separate parallel project, still pending.)
  Zote is a separate Python bot by the same user — see /home/prizmo/zote.

── CURRENT SESSION (2026-05-29, session 9) ───────────────────────────────────

  Prismarine viewer rebuild — PHASE 1 COMPLETE.

  All blocks now render correctly in the browser viewer:
  - Vanilla blocks: real textures (correct stateId mapping via prominence-registry.json)
  - Modded blocks: hash-colored solid cubes (phase 1 placeholder)

  Key files created/modified:
  - build-viewer-assets.js    — run once (server online) to regenerate all assets
  - nilo-assets/prominence-registry.json   — 618k stateId→name array (~19MB, ~2MB gzipped)
  - nilo-assets/prominence-blockstates.json — baked model JSON for prismarine-viewer
  - nilo-assets/prominence-atlas.png        — 4096×4096 texture atlas
  - node_modules/prismarine-viewer/…        — patched source + webpack rebuild needed:
      lib/world.js, lib/worker.js, lib/worldrenderer.js, lib/viewer.js,
      lib/index.js, lib/mineflayer.js

  Root causes fixed:
  1. Vanilla stateId shift: 22,117 vanilla stateIds differed from mcData because
     mods add extra properties (waterlogging, etc.) to early vanilla blocks.
     Fixed by serving prominence-registry.json (from /all-blocks) to the browser.
  2. Modded blocks missing: 17,437 unique modded block names now have blockStates
     entries + atlas tiles.

  PHASE 2 COMPLETE (same session):
  - build-mod-textures.js extracts assets from 95/110 mod JARs (456 total,
    15 mods had no extractable blockstate assets).
  - 5,019 modded blocks now render with real baked textures.
  - 12,418 still use colored cube fallback (missing JAR or unresolved model chain).
  - Script is offline-capable: caches /all-blocks → nilo-assets/all-blocks-cache.json.
  - Outputs deployed to prismarine-viewer/public/ automatically on each run.

  NEXT (optional phase 3): investigate missing 15 mod JARs and fix unresolved
  model chains to push real-texture coverage higher.

── PREVIOUS SESSION (2026-05-28–29, session 8) ───────────────────────────────

  Block learning, CLI session, bug fixes, status/stats commands.

  Auto-mapper reconnect fix (registry-patch.js):
  - Root bug: vanillaMax recomputed from already-patched registry on reconnect
    (Node module cache shares registry object). Gave 616281 instead of 24134,
    excluding all modded stateIds from gap analysis forever.
  - Fix 1: `if (!vanillaMax)` guard — computed only once at first clean spawn.
  - Fix 2: removed `!bot.registry.blocksByStateId[id]` from resolveMapping
    filter — was silently excluding all already-patched IDs on reconnect.
  - Confirmed working: all reconnects now show vanillaMax=24134 in registry.log.

  Ground-truth block learning (context-mod-client.js, new):
  - Zote's server-side Fabric mod extended: GET /blocknames?sids=n1,n2,...
    returns {"stateId":"blockName"} using Block.STATE_IDS + Registries.BLOCK.
  - context-mod-client.js polls every 15s, batches 200 sids per request.
  - applyGroundTruth() in registry-patch: 'ground_truth' confidence, logs
    every mismatch with gap analysis. Confirmed: 1000-1077 corrections/session.
  - Fabric mod rebuilt as v1.1.0 and deployed to server mods folder.

  Terminal CLI session (nilo-cli.js):
  - WebSocket server (ws://localhost:4000) in nilo.js replaces old HTTP API.
  - bot.chat() monkey-patched on login to forward replies to CLI clients.
  - /home/prizmo/.local/bin/nilo — interactive chat REPL with colored output,
    auto-reconnects after Nilo restart (retries every 3s).

  New commands (commands/misc.js):
  - "this is <mod:name>": sets manual override for gaze-target stateId.
  - "status": pings Letta, context mod, viewers, reports mapper stats,
    behavior, position, server. All checks run in parallel.
  - "stats": health as X%  (current/max, real max from generic.max_health
    attribute — correct for modpack-expanded health pools), hunger %,
    armor defense points (from generic.armor attribute), XP level.
  - "restart" → "Restarting..." / "reboot" → "Rebooting..." (distinct msgs).

  Navigation (commands/navigation.js):
  - "tp <p1> to <p2>": general handler. Resolves "me"→MASTER, "you"→NILO.
    Placed before IS_TP_TO_ME to prevent "tp me to Zote" misfiring.

  Follow behavior (movement.js):
  - >100 blocks from master while following → teleports instead of pathfinding.
  - Door spam fix: 2-block tall doors shared one cooldown via base-y key
    (x, floor(y/2), z). Old per-block 0.8s cooldown caused infinite loop
    alternating between upper and lower halves. Now 3s shared cooldown.

  Chat filter (nilo.js):
  - Messages starting with "!" (not "!nilo") silently dropped.

  Browser views (viewer.js):
  - prismarine-viewer: 3D world view → http://localhost:3007
  - mineflayer-web-inventory: inventory → http://localhost:3000
  - World viewer now closes old instance before restart (port release + 300ms
    wait). Inventory viewer auto-stops on bot.end. Both restart cleanly.
  - Accessible via Tailscale IP directly (servers bind 0.0.0.0).

── PREVIOUS SESSION (2026-05-25, session 7) ──────────────────────────────────

  Planning session — mirror feature design.
  Decision: server-side Fabric mod intercepts C2S packets, forwards to Nilo
  via WebSocket. NOT YET IMPLEMENTED — in CHECKLIST.txt.

── DEFERRED ──────────────────────────────────────────────────────────────────

  See CHECKLIST.txt → NEEDS LIVE TEST and KNOWN GAPS.
