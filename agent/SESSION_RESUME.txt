SESSION RESUME — 2026-08-05 (session 17)
=============================================
Pending items: agent/CHECKLIST.txt
Full architecture: agent/map.txt
Detailed history: agent/CHANGELOG.txt

── CURRENT SESSION (2026-08-05, session 17) ───────────────────────────────────

  Objective pivot: stop chasing new features, audit what's already claimed
  as built and fix what's actually broken — several "done" mechanics turned
  out not to work. NOTHING below has been live-tested yet — see
  CHECKLIST.txt's new "SESSION 17" section for the full test punch list.
  Highlights:

  - Totem fix: was reactive (only tried to equip below a health threshold,
    only inside attack/assist mode) — too late for most real deaths and
    dead outside active combat. Replaced with mineflayer-totem-auto
    (proactive, physicsTick-based, npm package — vetted source before
    installing). Dead reactive code removed from combat.js.
  - Possession mode (remote-control.js, driven by prizmo-system's
    BotSneakScreen): was already correctly built, but stopRemoteControl()
    was never called on disconnect — its module-level poll guard stayed
    stuck pointing at the dead bot object after any reconnect, silencing
    possession for the rest of the process with no error logged. Fixed by
    wiring it into nilo.js's bot.on('end', ...).
  - Full pathfinder engine swap: mineflayer-pathfinder (unmaintained since
    2023) → @miner-org/mineflayer-baritone, across all 27 files that touched
    it. Built pathfinder-compat.js, a shim reproducing the old API
    (setGoal/goto/setMovements/.goal/.movements/.stop, Movements,
    GoalNear/GoalBlock/GoalFollow with old constructor signatures) backed by
    the new engine, so ~150+ call sites needed only an import-line change
    instead of bespoke rewrites. Along the way: caught GoalFollowEntity
    isn't actually exported by the new library (built GoalFollow on GoalNear
    instead, matching how the library's own follow helper works internally)
    and found mineflayer-collectblock is incompatible with the new engine
    (constructs goals from its own bundled OLD pathfinder, calls a Movements
    method the shim doesn't have) — replaced its 3 real call sites with a
    new collectBlock(bot, block) helper in movement.js, removed the plugin.
    mineflayer-tool's equipForBlock has the same latent risk on an
    unconfirmed-unused code path — flagged, not fixed.
  - mineflayer-spectator wired in (nilo.js) — real-MC-client camera-follow
    spectate, port 25566, password nilo123, reconnect-safe (previous server
    instance is closed before a new one starts on each reconnect — same bug
    class as the possession fix, caught proactively this time).
  - Command prefix: renamed !/!nilo → #/#nilo everywhere (in-game chat,
    Discord bridge, MANUAL.txt). More importantly, changed the actual
    behavior: commands/index.js's handleNaturalCommand now REQUIRES
    prefixed:true to match anything at all — "quick" commands (follow, sit,
    mine this, etc.) used to fire on bare natural language with no prefix,
    which was the real source of accidental activation during casual chat.
    Local CLI and prizmo-system's terminal command box are deliberately
    exempted (dedicated command consoles, not casual chat).
  - Passwords (NILO_PASSWORD/SPECTATOR_PASSWORD) reviewed: both currently
    fall back to a hardcoded 'nilo123' literal in committed source (this
    repo has a GitHub remote) since .env doesn't set either. User chose to
    leave as-is (home LAN only, not port-forwarded) — revisit if that
    network exposure ever changes.

── PREVIOUS SESSION (2026-06-07, session 16) ──────────────────────────────────

  Clone army polish — formation, real skins, Solsai-based inventory
  management, HUD integration, "clone N" resizing. See CHANGELOG for full
  details. Highlights:

  - Replaced GoalFollow (made every clone converge on one point and stack
    "inside each other") with a stable per-index ring formation around Nilo
    (bot._formationAngle) plus mutual personal-space avoidance — verified
    spread-out (~1.75-8 blocks apart across 6 clones).
  - Fixed clone skins: "/skin set <url>" silently failed because
    mineskin.org can't reach the LAN-only IP 192.168.1.100. Switched to
    FabricTailor's "/skin set upload slim <server-side path>" (reads the
    PNG straight off the MC server's disk — found via decompiling
    fabrictailor-2.1.2.jar). Verified texture hash matches Nilo's real skin.
  - Refactored Freyr Sword distribution AND added new bread/food
    distribution, both via Solsai's instant /item-transfer (server-side
    inventory-to-inventory move) instead of the old toss+/tp+pickup dance —
    no more manual supply, nothing can be lost/stolen by mobs en route.
    Each clone gets topped up to ~16 bread once it drops to <=10.
  - HUD fix: clones were always showing as 'idle'/wrong-color in
    prizmo-system because nothing pushed their behavior mode to Solsai.
    Generalized api.js's pushModeToSolsai(mode) -> (player, mode) and added
    a 2s poll in clones.js pushing each clone's live mode — ESP colors and
    inventory peek now work for the whole army.
  - "clone N" now resizes the army in both directions: requesting fewer
    clones than currently spawned disbands the highest-indexed ones first
    (retracting Freyr Swords first). User/linter follow-up same session
    extended this to accept "clone 0" as a full stand-down without
    deactivating clone mode.
  - MANUAL.txt's EXÉRCITO DE CLONES section rewritten to document all of
    the above in Portuguese.

── PREVIOUS SESSION (2026-06-07, session 15) ─────────────────────────────────

  Freyr Sword reliability pass. See CHANGELOG for full details. Highlights:

  - Diagnosed why "activate freyr" / "freir return" did nothing but Nilo
    chatted as if they worked: commands/freyr.js was never registered in
    commands/index.js's handler list (clone-army commit added the file but
    not the wiring), and the "freir" typo bypassed the `includes('freyr')`
    gate — both fell through to Letta, which fabricated plausible-sounding
    fake confirmations. The router-wiring fix is already uncommitted in the
    working tree and live since the 01:01 restart — STILL NEEDS COMMITTING.
    The typo-tolerance fix is NOT yet done (see CHECKLIST).
  - Added freyr.retractBeforeDisconnect(bot) — silently recalls the sword
    before every controlled logoff (chat leave/disconnect, !nilo quit,
    nilo stop/restart via SIGTERM/SIGINT). Fixes the root cause of the
    "Nilo thinks his sword is summoned but it's not really his" stuck state
    (UUID binding never clears, so a sword left out across a logout gets
    bound to forever on the next login).
  - Added freyr.findStraySwords() + a ~30s scan in monitor.js's autonomous
    loop — flags freyr_sword_entity nearby that aren't Nilo's and don't
    belong to any online clone (i.e. likely abandoned by a dead/disconnected
    clone). Logs to freyr.log only, no chat.
  - "clone 0" is now valid — disbands the whole army without deactivating
    clone mode (was previously floored to spawn 1 clone). MANUAL.txt updated.

── STANDARD NILO COMMANDS ────────────────────────────────────────────────────

  Nilo runs as a systemd user service. Never start it with `node nilo.js`.

  nilo start     → start the bot
  nilo stop      → stop the bot
  nilo restart   → restart the bot
  nilo status    → service status
  nilo logs      → follow live logs (Ctrl+C to exit)
  nilo           → open interactive chat session

  Service file: ~/.config/systemd/user/nilo.service
  Survives terminal disconnects, SSH drops, Termux crashes.
  Auto-restarts on failure (10s delay). Starts on boot (linger enabled).

── OBJECTIVE FOR NEXT SESSION ────────────────────────────────────────────────

  NILO V1 — Node.js bot, no learning-mode constraints. Just implement.
  (Nilo v2 Python rewrite is a separate parallel project, still pending.)
  Zote is a separate Python bot by the same user — see /home/prizmo/zote.

── CURRENT SESSION (2026-06-06, session 14) ──────────────────────────────────

  Bug fixes — wander leash, grave detection, behavior persistence.

  WANDER LEASH (monitor.js, commands/navigation.js, nilo.js):
  - Nilo no longer wanders when MASTER is offline or >30 blocks away.
  - Guard added to all three wander sources: autonomous exploration (monitor.js),
    "wander" command interval (navigation.js), and wander restoration on spawn (nilo.js).
  - Autonomous exploration step capped at ±10 blocks (was ±20).
  - Root cause: wander picks random offset from current pos every few seconds —
    compounds unboundedly over time. Now halts unless MASTER is nearby.

  GRAVE BUG FIX (activities.js):
  - isGraveBlock() catch-all for unnamed/unknown blocks now only fires when
    yigd:grave couldn't be resolved (graveBlockId === null).
  - Previously matched any solid block with an empty name — including gravel
    variants with unresolved stateIds. Nilo was walking to gravel thinking it
    was his grave.

  BEHAVIOR PERSISTENCE FIX (persist.js, nilo.js):
  - persist.js: saveBehavior() now also saves exploringEnabled from state.
    loadBehavior() returns it. exploringEnabled is restored on spawn.
  - Bug fixed: exploringEnabled always reset to true on reboot (state.js default),
    so Nilo would start autonomous exploring even after "stop exploring."
  - Bug fixed: saved mode 'idle' (from "stay") now suppresses auto-follow on
    reboot. Previously skipAutoFollow only checked sit/defensive/passive, so
    "stay" → reboot → Nilo immediately followed when MASTER appeared.
  - Bug fixed: wander restoration in spawn now has the 30-block MASTER guard.

  PENDING (carried from session 13):
  - prizmo-system 0.1.14 jar needs to be deployed to Apollo's mods folder.
  - DuckDNS: waiting for token.
  - Self-bot Discord: waiting for token.
  - Config screen for prizmo-system: needs ModMenu API dependency.
  - Mirror mode spatial rewrite: needs user decision (fixed vs dynamic plane).

── PREVIOUS SESSION (2026-06-06, session 13) ─────────────────────────────────

  Major checklist pass — prizmo-system HUD mod + Nilo bot features. See CHANGELOG.

  Solsai 1.8.0, prizmo-system 0.1.14 HUD, navigation location commands,
  diggingCompleted ground-truth learning, api.js mode push to Solsai.
  See CHANGELOG for full details.

── DEFERRED ──────────────────────────────────────────────────────────────────

  See CHECKLIST.txt → NEEDS LIVE TEST and KNOWN GAPS.
